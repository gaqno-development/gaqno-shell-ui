version: '3.8'

services:
  shell:
    build:
      context: .  # Repository root (standalone)
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - VITE_PUBLIC_SUPABASE_URL=${VITE_PUBLIC_SUPABASE_URL}
      - VITE_PUBLIC_SUPABASE_ANON_KEY=${VITE_PUBLIC_SUPABASE_ANON_KEY}
      # Micro-Frontend Service URLs (for Coolify internal network)
      - VITE_SSO_SERVICE_URL=${VITE_SSO_SERVICE_URL:-http://localhost:4001}
      - AI_SERVICE_URL=${AI_SERVICE_URL:-http://localhost:3002}
      - CRM_SERVICE_URL=${CRM_SERVICE_URL:-http://localhost:3003}
      - ERP_SERVICE_URL=${ERP_SERVICE_URL:-http://localhost:3004}
      - FINANCE_SERVICE_URL=${FINANCE_SERVICE_URL:-http://localhost:3005}
      - PDV_SERVICE_URL=${PDV_SERVICE_URL:-http://localhost:3006}
    env_file:
      - .env.local  # Local env file (or set in Coolify)
    restart: unless-stopped
    networks:
      - gaqno-network

networks:
  gaqno-network:
    driver: bridge

